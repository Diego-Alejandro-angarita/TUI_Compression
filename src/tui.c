#include "tui.h"

int tui_init_stub(void)
{
    /* TODO: Integrar ncurses y editor cuando el pipeline base este estable. */
    return 0;
}

int tui_run_stub(void)
{
    /* Stub temporal: no inicializa ncurses ni edita texto todavia. */
    return tui_init_stub();
}
